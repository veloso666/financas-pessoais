# Financas Pessoais V1

App Streamlit + PostgreSQL para controle de gastos.

## Setup

1. Crie o banco: CREATE DATABASE financas;
2. Copie .env.example para .env e preencha suas credenciais
3. python -m venv venv && venv\Scripts\activate
4. pip install -r requirements.txt
5. streamlit run app.py
